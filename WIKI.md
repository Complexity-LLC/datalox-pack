# Wiki Instructions

This repo uses a reduced knowledge model:

- `skills/` for workflows
- `agent-wiki/notes/` for reusable knowledge pages

Read the selected skill, then the linked notes.
Prefer one strong note with rule plus evidence over multiple generated page types.

Legacy folders may still exist in older repos, but new automatic writes should go to `agent-wiki/notes/`.
